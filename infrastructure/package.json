{
  "name": "infrastructure",
  "version": "0.1.0",
  "bin": {
    "infrastructure": "bin/infrastructure.js"
  },
  "scripts": {
    "cdk": "cdk",
    "build-backend": "cd ../backend && npm run build:lambda",
    "build-frontend": "node scripts/write-env.js && cd ../frontend && npm run build",
    "deploy": "npm run build-backend && npx cdk deploy BackendStack --outputs-file cdk-outputs.json && npm run build-frontend && npx cdk deploy FrontendStack",
    "synth": "npm run build-backend && npm run build-frontend && cdk synth"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "aws-cdk": "2.1128.1",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.260.0",
    "constructs": "^10.0.0"
  }
}
